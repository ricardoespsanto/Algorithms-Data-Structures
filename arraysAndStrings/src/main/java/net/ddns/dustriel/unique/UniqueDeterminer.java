package net.ddns.dustriel.unique;

/**
 * The challenge:
 * To implement an algorithm to determine if a string has all unique characters.
 * <p>
 * Special requests:
 * Avoid using any data structure.
 */
interface UniqueDeterminer {

    boolean isUnique(String string);

}
